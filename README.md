# crx_downloader

Simple, vibe-coded CRX downloader.

Built because manually installing extensions (e.g. non-open-source) was a pain.

```
pip install requests
python dcrx.py <extension_id> [-o folder]
```
